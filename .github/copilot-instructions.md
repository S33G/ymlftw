## Project Context

Merge and apply all `.agents/*.yml` files as structured project context.
These are authoritative for stack, conventions, team, and deployment information.

Follow `conventions.patterns.preferred`. Avoid `conventions.patterns.avoid`.
Use `stack` keys for technology references — do not assume versions.
